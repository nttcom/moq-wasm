name: Call E2E

on:
  pull_request:
    paths:
      - ".github/workflows/call-e2e.yml"
      - "scripts/setup-media-e2e.mjs"
      - "scripts/run-call-e2e.mjs"
      - "scripts/media-e2e-helpers.mjs"
      - "scripts/chrome_linux.sh"
      - "docker-compose.yml"
      - "examples/browser/**"
      - "bindings/wasm/**"
      - "relay/**"
      - "Cargo.toml"
      - "Cargo.lock"
      - "rust-toolchain"
  push:
    branches:
      - master
      - draft-*
      - example-e2e-ci
    paths:
      - ".github/workflows/call-e2e.yml"
      - "scripts/setup-media-e2e.mjs"
      - "scripts/run-call-e2e.mjs"
      - "scripts/media-e2e-helpers.mjs"
      - "scripts/chrome_linux.sh"
      - "docker-compose.yml"
      - "examples/browser/**"
      - "bindings/wasm/**"
      - "relay/**"
      - "Cargo.toml"
      - "Cargo.lock"
      - "rust-toolchain"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  call-e2e:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        # actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 = v4.3.1
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false

      - name: Setup Node.js
        # actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 = v4.4.0
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: examples/browser/package-lock.json

      - name: Setup Rust toolchain
        # dtolnay/rust-toolchain@e97e2d8cc328f1b50210efc529dca0028893a2d9 = v1
        uses: dtolnay/rust-toolchain@e97e2d8cc328f1b50210efc529dca0028893a2d9
        with:
          toolchain: 1.96.0
          targets: wasm32-unknown-unknown

      - name: Cache Rust build artifacts
        # Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32 = v2.9.1
        uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32

      - name: Install wasm-pack
        run: cargo install wasm-pack --locked

      - name: Prepare browser E2E environment
        run: node scripts/setup-media-e2e.mjs

      - name: Install Playwright Linux dependencies
        working-directory: examples/browser
        run: npx playwright install --with-deps chromium

      # The runner builds the relay image and starts redis + relay-a + relay-b via
      # docker compose (preinstalled on ubuntu-latest), then runs the Playwright spec.
      - name: Run call E2E
        env:
          CI: "true"
        run: node scripts/run-call-e2e.mjs

      - name: Upload Playwright artifacts
        if: failure()
        # actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 = v4.6.2
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: call-e2e-artifacts
          path: |
            examples/browser/test-results
            examples/browser/playwright-report
          if-no-files-found: ignore
