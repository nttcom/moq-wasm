comment:
  layout: "header, diff, flags, components" # show component info in the PR comment

github_checks:
  annotations: false

coverage:
  status:
    project:
      default:
        target: 50%
        threshold: 10%
        informational: true
    patch:
      default:
        target: 70%
        threshold: 10%
        informational: true
