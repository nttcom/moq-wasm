# ONVIF credentials for Makefile targets
ONVIF_IP=
ONVIF_USERNAME=
ONVIF_PASSWORD=

# Relay authentication.
# Production: AUTH_VTS_URL=https://vts.internal/verify and AUTH_RELAY_TOKEN=<relay JWT>.
# Local development and e2e: opt out explicitly. The relay refuses to start
# when neither is set.
AUTH_DISABLED=true
# To run the local compose stack with authentication enabled:
#   docker compose --profile auth up -d
#   AUTH_RELAY_TOKEN=$(node services/vts/bin/mint.mjs --apps services/vts/apps.example.json \
#     --app-id 11111111-2222-3333-4444-555555555555 --publish "" --subscribe "" --ttl 8760h)
# AUTH_VTS_URL=http://vts:8081/verify
# AUTH_RELAY_TOKEN=
# ANON_SECRET=local-dev-secret-for-anon-change-me
# ANON_ALLOWED_ORIGINS=http://127.0.0.1:5173,http://localhost:5173

# MoQ bridge
MOQT_URL=https://localhost:4433

# OpenTelemetry / OTLP SaaS backend, for example Honeycomb.
OTEL_EXPORTER_OTLP_PROTOCOL=http/protobuf
OTEL_EXPORTER_OTLP_ENDPOINT=https://api.honeycomb.io
# Set headers required by your backend.
# Honeycomb example:
# OTEL_EXPORTER_OTLP_HEADERS=x-honeycomb-team=your-api-key,x-honeycomb-dataset=moqt-relay
OTEL_EXPORTER_OTLP_HEADERS=x-honeycomb-team=your-api-key,x-honeycomb-dataset=moqt-relay
OTEL_SERVICE_NAME=relay
OTEL_SDK_DISABLED=false

# Logging filters
RELAY_STDOUT_FILTER=relay=info,moqt=info
RELAY_OTEL_FILTER=relay=info,moqt=info
RELAY_LOG_FILTER=relay=info,moqt=info
